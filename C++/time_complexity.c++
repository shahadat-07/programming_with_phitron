#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
    
    // Comparing ASCII values of a and b
    cout << max('a','b') << "\n";
  
    // Returns the first one if both the numbers
    // are same
    cout << max({7,70, 100});
  
return 0;
} 